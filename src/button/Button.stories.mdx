import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { withKnobs, select } from '@storybook/addon-knobs';

import Button from './Button';

<Meta title="Wallet Roshi Design System | Button" component={Button} />

# Button

## Interactive Button

Buttons for user to interact with

<Preview withToolbar>
  <Story name="standard">
    <Button
      onClick={action('clicked')}
      color={select('Type', ['primary', 'secondary'])}
    >
      Button
    </Button>
  </Story>
</Preview>

<Props of={Button} />

### Standard Buttons

<Story name="colors">
  <div className="horizontal-box">
    <Button
      onClick={action('clicked')}
      color="primary"
    >
      Primary
    </Button>
    <Button
      onClick={action('clicked')}
      color="secondary"
    >
      Secondary
    </Button>
    <Button
      onClick={action('clicked')}
      color="success"
    >
      Success
    </Button>
    <Button
      onClick={action('clicked')}
      color="danger"
    >
      Danger
    </Button>
    <Button
      onClick={action('clicked')}
      color="warning"
    >
      Warning
    </Button>
    <Button
      onClick={action('clicked')}
      color="info"
    >
      Info
    </Button>
    <Button
      onClick={action('clicked')}
      color="light"
    >
      Light
    </Button>
    <Button
      onClick={action('clicked')}
      color="dark"
    >
      Dark
    </Button>
    <Button
      onClick={action('clicked')}
      color="muted"
    >
      Muted
    </Button>
  </div>
</Story>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="knobs" parameters={{ decorators: [withKnobs] }}>
  <Button
    onClick={action('clicked')}
    color={select('Type', ['primary', 'secondary'])}
  >
    Playground
  </Button>
</Story>
