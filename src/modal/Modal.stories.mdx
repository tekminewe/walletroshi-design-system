import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select } from '@storybook/addon-knobs';

import Modal from './Modal';
import { Card } from '../card';

<Meta title="Wallet Roshi Design System | Modal" component={Modal} />

# Modal

<Preview withToolbar>
  <Story name="Example" decorators={[withKnobs]}>
    <div>
      <Modal visible={select('Type', [
        true,
        false,
      ])}>
        <Card>
          <p>Good</p>
        </Card>
      </Modal>
    </div>
  </Story>
</Preview>
