import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import Input from './Input';
import Form from './Form';
import { Button } from '../button';
import { Spacer } from '../layout';
import { Card}  from '../card';
import { Text } from '../typography';

<Meta title="Wallet Roshi Design System | Form" />

# Form

All the elements that required to build Form

## Input

Text input that allow user to insert their value

<Preview withToolbar>
  <Story name="Input">
    <Card>
      <Form
        title="Sign in"
        subtitle="Welcome to Wallet Roshi"
      >
        <Input placeholder="Email address" name="email" type="email" required />
        <Spacer />
        <Input placeholder="Password" name="password" type="password" required />
        <Spacer />
        <Text>
          {`By signing in, I have read and agreed to Wallet Roshi's `}
          <a>Privacy Policy</a>
          {` and ` }
          <a>Terms of use</a>
          .
        </Text>
        <Spacer />
        <div className="full-width">
          <Button type="submit">Sign in</Button>
        </div>
      </Form>
    </Card>
  </Story>
</Preview>

<Story name="Error">
  <Input placeholder="Email address" name="email" type="email" required error="Email address is required" />
</Story>
